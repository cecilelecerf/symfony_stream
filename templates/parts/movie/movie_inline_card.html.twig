<li class=" transition cursor-pointer hover:bg-black/10 rounded">
	<a class="flex space-x-1" href={{path("detail", {"id": user_watchHistory.getMedia().getId()})}}>
		<img src={{user_watchHistory.getMedia().getCoverImage()}} class="w-1/3 rounded-md" alt="">
		<div class="flex flex-col justify-between  ">
			<div class="flex flex-col space-y-1">
				<span class="text-gray-700 font-semibold mt-2">{{ user_watchHistory.getMedia().getTitle()}}</span>
				<span class="text-xxs hidden xl:block">Drama</span>
			</div>
			<div class="flex items-center">
				<p class="px-2 py-1 mb-2 text-white bg-red-600 hover:bg-red-700 rounded-lg text-xxs inline-block">Vu
					<span>{{user_watchHistory.numberOfViews}}</span>
				</p>

			</div>
		</div>
	</a>
</li>
